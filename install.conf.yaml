- clean: ['~', ]

- defaults:
    link:
        force: true
        create: true
        relative: true

- link:
    ~/.vimrc:                  vimrc
    ~/.vim : vim

    ~/.zshrc: zshrc

    ~/.tmux.conf: tmux.conf

- shell:
    - [git submodule update --init --recursive, Installing submodules]
